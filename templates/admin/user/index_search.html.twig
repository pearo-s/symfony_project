<table class="table table-hover">
    <thead class="table-secondary">
    <tr>
        <th {{ app.request.query.has('sort') and app.request.query.get('sort') == 'u.id' ? 'class="text-danger font-italic"' : ''}} scope="col">ID</th>
        <th {{ app.request.query.has('sort') and app.request.query.get('sort') == 'u.name' ? 'class="text-danger font-italic"' : ''}} scope="col">Name</th>
        <th {{ app.request.query.has('sort') and app.request.query.get('sort') == 'u.surname' ? 'class="text-danger font-italic"' : ''}} scope="col">Surname</th>
        <th {{ app.request.query.has('sort') and app.request.query.get('sort') == 'u.username' ? 'class="text-danger font-italic"' : ''}} scope="col">Username</th>
        <th colspan="2" class="text-center">Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for user in pagination %}
        <tr>
            <th scope="row">{{ user.id }}</th>
            <td>{{ user.name }}</td>
            <td>{{ user.surname }}</td>
            <td>{{ user.username }}</td>
            <td class="d-flex justify-content-center gap-2">

                <a class="btn btn-secondary" href="{{ path('show_user', {'id': user.id}) }}">Details</a>

                <form action="{{ path('delete_user', {'id': user.id}) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                    <button type="submit" onclick="return confirmAction()" class="btn btn-danger">
                        Delete
                    </button>
                </form>

            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>