{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">

        <div class="row">
            <div class="col-12">
                <h1 class="text-center h2 my-3">User creation</h1>
            </div>
        </div>

        {{ form_start(form, {'attr': {'class': 'w-50'}}) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}

    </div>
{% endblock %}
