# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            enabled: true
            #token_id: submit

    csrf_protection:
        #stateless_token_ids:
        enabled: true
            #- submit
            #- authenticate
            #- logout
